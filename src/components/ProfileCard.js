import React from "react";

const ProfileCard = (props) => {
  const { testUser } = props;

  // will need to set up a boolean piece of state that can be used to change view from editable to static

  return (
    <div>
      <h1 className="text-center">Profile Card</h1>
    </div>
  )
}

export default ProfileCard;